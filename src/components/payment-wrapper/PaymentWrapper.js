import React from "react";
import styled from "styled-components";

const PaymentWrapper = () => {
  return <Wrapper>Payment Wrapper</Wrapper>;
};

export default PaymentWrapper;

const Wrapper = styled.div``;
